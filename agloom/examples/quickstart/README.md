# Quickstart

**`basic_agent.py`** — the simplest possible `create_agent` + `ainvoke` usage.

```bash
uv run basic_agent.py
```

No tools, no memory, no streaming — just a plain LLM call through `agloom`.
