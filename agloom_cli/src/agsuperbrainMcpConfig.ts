/** Written to ``.agloom/mcp/agsuperbrain.yaml`` — loaded via ``--mcp agsuperbrain:<path>`` (``MCPServerConfig``). */

export const AGSUPERBRAIN_MCP_CONFIG_YAML = `name: agsuperbrain
transport: stdio
command: agsuperbrain
args:
  - mcp-serve
timeout: 120.0
`
