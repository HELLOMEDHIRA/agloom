# Production / staging: point the SPA at your AGP runtime WebSocket endpoint.
# Defaults in dev: Vite proxies `/agp-ws` → ws://localhost:8765 (see vite.config.ts).
#
# Examples:
# VITE_AGP_WS_URL=wss://runtime.example.com/agp
# VITE_AGP_WS_URL=/agp-ws

VITE_AGP_WS_URL=
